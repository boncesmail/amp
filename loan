






<!DOCTYPE html>
<html amp lang="en-ID">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Slot777 : Market Link Slot Gacor 777 Penerima Member Baru MPO77</title>
  <meta name="description" content="Slot777 Market Link Slot Gacor 777 dengan akses praktis dan informasi terbaru untuk member baru. Nikmati pengalaman bermain dengan layanan yang mudah diakses dan nyaman. Gabung di Mpo77 sekarang." />
  <meta name="robots" content="index,follow" />
  <meta name="keywords" content="Mpo77, slot 4d, link Mpo77, slot777, slot gacor">
  <meta name="publisher" content="slot777">
  <link href="https://ik.imagekit.io/ctoqfgj0a/seokampung.png" rel="shortcut icon" type="image/x-icon" />
  <link rel="canonical" href="https://techizer360.com/" />
  <link rel="alternate" href="https://techizer360.com/" hreflang="id" />
  <script async src="https://cdn.ampproject.org/v0.js"></script>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@700;900&family=Montserrat:wght@300;400;600&display=swap" rel="stylesheet">

  <style amp-boilerplate>
    body {
      -webkit-animation: -amp-start 8s steps(1, end) 0s 1 normal both;
      -moz-animation: -amp-start 8s steps(1, end) 0s 1 normal both;
      -ms-animation: -amp-start 8s steps(1, end) 0s 1 normal both;
      animation: -amp-start 8s steps(1, end) 0s 1 normal both;
    }

    @-webkit-keyframes -amp-start {
      from { visibility: hidden }
      to { visibility: visible }
    }

    @-moz-keyframes -amp-start {
      from { visibility: hidden }
      to { visibility: visible }
    }

    @-ms-keyframes -amp-start {
      from { visibility: hidden }
      to { visibility: visible }
    }

    @-o-keyframes -amp-start {
      from { visibility: hidden }
      to { visibility: visible }
    }

    @keyframes -amp-start {
      from { visibility: hidden }
      to { visibility: visible }
    }
  </style>

  <noscript>
    <style amp-boilerplate>
      body {
        -webkit-animation: none;
        -moz-animation: none;
        -ms-animation: none;
        animation: none;
      }
    </style>
  </noscript>

  <style amp-custom>
    *{box-sizing:border-box}

    body{
      font-family:'Montserrat',sans-serif;
      background:
        linear-gradient(
          180deg,
          rgba(30,0,0,.4),
          rgba(10,0,0,.9)
        ),
        url("https://ik.imagekit.io/ctoqfgj0a/seojoganteng.jpg") center/cover no-repeat fixed;
      color:#fff;
      min-height:100vh;
      margin:0;
      padding:20px;
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
      overflow-x:hidden;
    }

    .overlay{
      position:fixed;
      inset:0;
      pointer-events:none;
      z-index:9999;
      overflow:hidden;
      animation:overlayHide 6s ease-in-out forwards;
    }

    .bg-gif{
      position:fixed;
      inset:0;
      width:100vw;
      height:100vh;
      opacity:0;
      animation:showGif 6s ease-in-out forwards;
      z-index:1;
    }

    .bg-gif amp-img,
    .center-gif amp-img{
      width:100%;
      height:100%;
    }

    .center-gif{
      position:absolute;
      left:50%;
      top:120%;
      transform:translate(-50%, -50%);
      width:28vw;
      max-width:260px;
      min-width:120px;
      opacity:0;
      z-index:2;
      filter:drop-shadow(0 0 35px gold);
      animation:flyUpCenter 6s ease-in-out forwards;
    }

    @keyframes flyUpCenter{
      0%{
        top:120%;
        opacity:0;
      }
      35%{
        top:55%;
        opacity:1;
      }
      85%{
        top:55%;
        opacity:1;
      }
      100%{
        top:55%;
        opacity:0;
      }
    }

    @keyframes showGif{
      0%{opacity:0;}
      30%{opacity:.35;}
      85%{opacity:.35;}
      100%{opacity:0;}
    }

    @keyframes overlayHide{
      0%{visibility:visible;}
      99%{visibility:visible;}
      100%{visibility:hidden;}
    }

    .imlek-glow{
      position:fixed;
      top:50%;
      left:50%;
      width:320px;
      height:320px;
      transform:translate(-50%,-50%);
      background:radial-gradient(circle,#ffd966 0%,transparent 70%);
      opacity:.12;
      filter:blur(90px);
      z-index:-1;
      animation:pulse 6s ease-in-out infinite;
    }

    @keyframes pulse{
      0%,100%{opacity:.1}
      50%{opacity:.2}
    }

    .logo-container{
      width:150px;
      height:150px;
      position:relative;
      margin-bottom:25px;
      display:flex;
      align-items:center;
      justify-content:center;
    }

    .logo-img{
      width:105px;
      filter:drop-shadow(0 0 15px rgba(255,215,102,.6));
      z-index:2;
    }

    h1{
      font-family:'Cinzel',serif;
      font-size:28px;
      letter-spacing:3px;
      margin:0;
      text-shadow:0 0 15px rgba(255,215,102,.6);
    }

    .sub-heading{
      margin:10px 0 30px;
      font-size:12px;
      letter-spacing:4px;
      color:#ffd966;
      border-bottom:1px solid rgba(255,215,102,.4);
      padding-bottom:8px;
      text-align:center;
    }

    .banner-card{
      max-width:380px;
      width:100%;
      border-radius:18px;
      overflow:hidden;
      margin-bottom:35px;
      position:relative;
      padding:3px;
      background:linear-gradient(
        135deg,
        #7a0c0c,
        #b11226,
        #ffd966,
        #fff1b0,
        #ffd966,
        #b11226,
        #7a0c0c
      );
      box-shadow:
        0 0 25px rgba(255,215,102,.45),
        0 18px 45px rgba(177,18,38,.7);
    }

    .banner-card amp-img{
      border-radius:15px;
    }

    .btn-stack{
      position:relative;
      z-index:2;
      max-width:380px;
      width:100%;
      display:flex;
      flex-direction:column;
      gap:18px;
    }

    .btn-card{
      background:rgba(20,0,0,.7);
      border:1px solid rgba(255,215,102,.25);
      border-radius:14px;
      padding:6px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      transition:.3s;
    }

    .btn-card:hover{
      transform:translateY(-3px);
      border-color:#ffd966;
      box-shadow:0 0 25px rgba(255,215,102,.3);
    }

    .btn-label{
      display:flex;
      align-items:center;
      gap:14px;
      padding-left:14px;
    }

    .icon-box{
      width:34px;
      height:34px;
      border-radius:50%;
      background:radial-gradient(circle,#ffd966,#fff600);
      display:flex;
      align-items:center;
      justify-content:center;
    }

    .btn-title{
      font-family:'Cinzel',serif;
      font-size:16px;
      color:#ffd966;
      letter-spacing:1px;
    }

    .btn-sub{
      font-size:10px;
      color:#ffcc99;
      letter-spacing:1px;
    }

    .action-link{
      padding:12px 30px;
      border-radius:8px;
      font-size:12px;
      font-weight:700;
      text-decoration:none;
      text-transform:uppercase;
      letter-spacing:1.2px;
    }

    .btn-outline{
      background:linear-gradient(135deg, #000000, #8d7a1b);
      color:#fff;
      box-shadow:0 0 20px rgba(255,215,102,.6);
    }

    .btn-solid{
      background:linear-gradient(135deg, #000000, #8d7a1b);
      color:#fff;
      box-shadow:0 0 20px rgba(255,215,102,.6);
    }

    .btn-link{
      border:1px solid #555;
      color:#fff;
    }

    .copyright{
      margin-top:40px;
      font-size:10px;
      letter-spacing:2px;
      color:#fff600;
      text-transform:uppercase;
    }

    .glass-effect{
      position:relative;
      overflow:hidden;
    }

    .glass-effect::after{
      content:"";
      position:absolute;
      top:0;
      left:-150%;
      width:120%;
      height:100%;
      background:
        linear-gradient(
          120deg,
          transparent 0%,
          rgba(255, 215, 0, 0.3) 40%,
          rgba(255, 215, 0, 0.3) 50%,
          rgba(255, 215, 0, 0.3) 60%,
          transparent 100%
        );
      transform:skewX(-15deg);
      animation:glassShine 4.5s ease-in-out infinite;
      pointer-events:none;
    }

    @keyframes glassShine{
      0%{left:-150%;}
      50%{left:150%;}
      100%{left:150%;}
    }

    .sakura-fall{
      position:fixed;
      top:-15%;
      left:-10%;
      width:120%;
      height:120%;
      pointer-events:none;
      z-index:1;
      overflow:hidden;
    }

    .sakura-fall::before,
    .sakura-fall::after{
      content:"";
      position:absolute;
      inset:0;
      background:
        radial-gradient(circle at 10% 0%, rgba(139, 90, 43, 0.9) 0 5px, transparent 6px),
        radial-gradient(circle at 35% 15%, rgba(160, 110, 60, 0.85) 0 6px, transparent 7px),
        radial-gradient(circle at 60% 5%, rgba(120, 72, 35, 0.9) 0 5px, transparent 6px),
        radial-gradient(circle at 85% 20%, rgba(181, 130, 75, 0.8) 0 6px, transparent 7px);
      background-size:320px 320px;
      animation:sakuraFall 22s linear infinite;
      opacity:.75;
    }

    .sakura-fall::after{
      background-size:420px 420px;
      animation:sakuraFallSlow 32s linear infinite;
      opacity:.45;
    }

    @keyframes sakuraFall{
      0%{
        transform:translateY(-20%) translateX(0);
      }
      100%{
        transform:translateY(120%) translateX(18%);
      }
    }

    @keyframes sakuraFallSlow{
      0%{
        transform:translateY(-25%) translateX(-5%);
      }
      100%{
        transform:translateY(120%) translateX(12%);
      }
    }

    .imlek-premium-frame{
      position:fixed;
      inset:0;
      pointer-events:none;
      z-index:0;
      overflow:hidden;
    }

    .imlek-premium-frame::before{
      content:"";
      position:absolute;
      inset:0;
      background:
        radial-gradient(circle at 8% 50%, rgba(255,215,150,.18) 0 120px, transparent 130px),
        radial-gradient(circle at 12% 60%, rgba(255,200,120,.14) 0 90px, transparent 100px),
        radial-gradient(circle at 92% 48%, rgba(255,215,150,.18) 0 120px, transparent 130px),
        radial-gradient(circle at 88% 62%, rgba(255,200,120,.14) 0 90px, transparent 100px);
      filter:blur(2px);
    }

    .imlek-premium-frame::after{
      content:"";
      position:absolute;
      inset:-20%;
      background:
        radial-gradient(circle, rgba(255,215,102,.8) 1px, transparent 2px),
        radial-gradient(circle, rgba(255,235,160,.6) 1px, transparent 2px);
      background-size:160px 160px, 220px 220px;
      animation:goldFloat 28s linear infinite;
      opacity:.35;
    }

    @keyframes goldFloat{
      from{
        transform:translateY(0);
      }
      to{
        transform:translateY(-160px);
      }
    }
  </style>
</head>

<body>

  <div class="overlay">
    <div class="bg-gif">
      <amp-img
        src="https://i.gyazo.com/6fa20696c5cde97ae2789806b6cb1d4a.gif"
        width="1920"
        height="1080"
        layout="fill"
        alt="Background Animation">
      </amp-img>
    </div>

    <div class="center-gif">
      <amp-img
        src="https://i.gyazo.com/105b485b4b17cad937ff0840246a3403.gif"
        width="350"
        height="300"
        layout="responsive"
        alt="Center Animation">
      </amp-img>
    </div>
  </div>

  <div class="imlek-premium-frame"></div>
  <div class="sakura-fall"></div>
  <div class="imlek-glow"></div>

  <amp-img
    id="image"
    class="animated tada infinite slower"
    src="https://ik.imagekit.io/ctoqfgj0a/seokampung.png"
    alt="GACOR Logo"
    width="336"
    height="200"
    layout="intrinsic"
    style="margin-bottom: 10px;">
  </amp-img>

  <div class="banner-card glass-effect">
    <amp-img
      src="https://ik.imagekit.io/ctoqfgj0a/seojoganteng.jpg"
      width="500"
      height="500"
      layout="responsive"
      alt="Banner GACOR">
    </amp-img>
  </div>

  <div class="btn-stack">
    <div class="btn-card">
      <div class="btn-label">
        <div>
          <div class="btn-title">LOGIN GACOR</div>
          <div class="btn-sub">Akun Special VIP</div>
        </div>
      </div>
      <a class="action-link btn-outline" href="https://mpo77.info/">DISINI</a>
    </div>

    <div class="btn-card">
      <div class="btn-label">
        <div>
          <div class="btn-title">DAFTAR GACOR</div>
          <div class="btn-sub">Persentase Winrate 99,99%</div>
        </div>
      </div>
      <a class="action-link btn-solid" href="https://mpo77.info/">DISINI</a>
    </div>
  </div>

  <div class="copyright">SEO © JO</div>

</body>
</html>

